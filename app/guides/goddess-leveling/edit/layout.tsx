import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Edit goddess upgrade order",
  robots: { index: false, follow: false },
};

export default function EditGoddessLevelingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
